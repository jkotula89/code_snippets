How to set up datalab?

1. Activate Compute engine (search in API)
2. Configurate and start a compute engine
3. Active the Cloud shell/or connect to via the google SDK to the account (gcloud auth login)°
4. In the shell, type in:
  * gcloud config set core/project myproject
  * glcoud config set compute/zone europe---- (select a default compute zone)
  * datalab create --no-create-repository notebook_name
  
  
How to use Tensorboard in datalab?

import google.datalab.ml as ml

[...] # your code

filewriter = tf.FileWriter('./dir')
filewriter.close()

tensorboard_pid = ml.Tensorboard.start('./dir')
ml.Tensorboard.stop(tensorboard_pid)
